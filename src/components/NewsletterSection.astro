---
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
---

<section id="newsletter" class="w-full py-12 md:py-24 lg:py-32 bg-primary-50 dark:bg-primary-900">
  <div class="container px-4 md:px-6">
    <div class="flex flex-col items-center space-y-4 text-center">
      <h2 class="text-3xl font-bold tracking-tighter sm:text-4xl md:text-5xl text-primary-800 dark:text-primary-200">
        Suscríbete a Nuestro Newsletter
      </h2>
      <p class="mx-auto max-w-[700px] text-gray-700 md:text-xl dark:text-gray-300">
        Recibe las últimas noticias y artículos directamente en tu bandeja de entrada.
      </p>
      <form class="flex w-full max-w-sm items-center space-x-2">
        <Input
          type="email"
          placeholder="Ingresa tu email"
          className="flex-1"
        />
        <Button type="submit">Suscribirse</Button>
      </form>
    </div>
  </div>
</section>